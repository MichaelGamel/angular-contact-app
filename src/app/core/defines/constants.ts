import { environment } from '@env/environment';

export const CONTACTS_KEY = `${environment.localStoragePrefix}${'contacts'}`;
export const CONTACTS_METADATA_KEY = `${environment.localStoragePrefix}${'contactsMetadata'}`;
